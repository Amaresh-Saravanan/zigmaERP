# Optional. djangoClient defaults to same-origin "/api", which works in dev via
# the Vite proxy (vite.config.js → http://localhost:8000) and in prod behind a
# reverse proxy. Only set this if the backend lives on a different origin.
# VITE_API_URL=http://127.0.0.1:8000/api
